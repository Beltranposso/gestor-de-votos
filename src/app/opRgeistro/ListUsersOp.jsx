import React, { useState, useEffect } from 'react';

const ListUsersOp = () => {
  const [data, setData] = useState([]);

  useEffect(() => {
    // Lógica de efectos aquí
  }, []);

  return (
    <div className="container mx-auto p-4">
        
    </div>
  );
};

export default ListUsersOp;
